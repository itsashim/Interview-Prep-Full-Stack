# Node debugger
The fundamental aspect of debugging is to set breakpoints.
Breakpoints are the points in our code that we can define in our debugger where our code will stop running, it will freeze in time so we can take a look in our variable and is extremely useful to find bugs.

